version: "3.9"

services:

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    container_name: bank_marketing_api
    ports:
      - "8000:8000"
    volumes:
      - ../logs:/app/logs
    environment:
      - MODEL_PATH=/app/models/best_model.pkl
    restart: unless-stopped